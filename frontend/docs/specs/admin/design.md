# AdminPage — Design

## Arquivos

- `src/pages/AdminPage.jsx`

## Implementação

A página não passa pelo `AuthContext`. O backend monta rotas administrativas em `/api/panel`: `POST /panel/login`, `GET /panel/stats` e `GET /panel/customers`.
