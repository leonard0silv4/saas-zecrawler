# ML SmartHub Frontend — Design do Índice

Esta pasta funciona como índice principal da documentação frontend.

As specs detalhadas ficam em pastas irmãs dentro de `frontend/docs/specs/`, cada uma com `requirements.md`, `design.md` e `tasks.md`.
