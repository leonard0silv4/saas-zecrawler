# SetupCookiesPage — Requirements

## Escopo

Fluxo guiado de configuração de cookies ML em `/setup-cookies`.

## Requisitos

- Deve aceitar JSON de cookies.
- Deve validar formato antes de enviar.
- Deve salvar cookies via `POST /cookies`.
- Deve atualizar status global de cookies após salvar.
